package com.yamaha.model.editor;

/**
 * A RMGroup represents a group of information to be saved in the Registration Memory.
 * <p>Note: Do not rename the enum fields because they are used via the MenuBarController to load the linked .fxml
 * files!
 */
public enum RMGroup { // extend this enum

    TITLE,
    SONG,
    STYLE,
    VOICE;

}